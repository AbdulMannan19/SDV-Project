python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
python.exe -m pip install --upgrade pip
python app.py

git status
git add .
git commit -m "quick commit"
git push


1) USA must have the highest ROI followed by Canada
2) Overall ROI must be 83.7%
3) Campaign Type must be ranked in the following order -
    Social Media
    Digital Ads
    Prints